{
  "kind": "collectionType",
  "collectionName": "matching_questions",
  "info": {
    "singularName": "matching-question",
    "pluralName": "matching-questions",
    "displayName": "Matching question"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "thumbnail": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": [
        "images"
      ]
    },
    "matching": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::matching.matching",
      "inversedBy": "matching_questions"
    },
    "matching_answer": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::matching-answer.matching-answer",
      "inversedBy": "matching_questions"
    },
    "audio_desc": {
      "type": "media",
      "multiple": false,
      "allowedTypes": [
        "audios"
      ]
    }
  }
}
