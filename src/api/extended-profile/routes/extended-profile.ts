/**
 * Extended profile router
 *
 * Standard CRUD routes for demographic profile records.
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::extended-profile.extended-profile');
