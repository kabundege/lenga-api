/**
 * Extended profile service
 *
 * Database access layer for extended profile collection records.
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::extended-profile.extended-profile');
