/**
 * Assessment submission service
 *
 * Database access layer for assessment submission collection records.
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::assessment-submission.assessment-submission');
