/**
 * Assessment submission controller
 *
 * Logs quiz and matching exercise outcomes for assessment analytics.
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::assessment-submission.assessment-submission');
