/**
 * Assessment submission router
 *
 * Standard CRUD routes for assessment outcome log entries.
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::assessment-submission.assessment-submission');
