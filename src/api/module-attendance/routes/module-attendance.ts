/**
 * Module attendance router
 *
 * Standard CRUD routes for lesson progress log entries.
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::module-attendance.module-attendance');
